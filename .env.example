# No environment variables are required. The game runs entirely in the browser.
# Optional: set this for canonical social preview URLs on a custom domain.
# NEXT_PUBLIC_SITE_URL=https://your-game.example
